import { form as Formulario, HeroTitle } from './styles'

const Hero = () => (
  <Formulario>
    <div className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </div>
  </Formulario>
)

export default Hero
